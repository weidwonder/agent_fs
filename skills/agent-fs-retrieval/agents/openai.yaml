interface:
  display_name: "Agent FS 检索执行器"
  short_description: "通过内置 CLI 调用 Agent FS 知识库服务，完成检索、回查与排障"
  default_prompt: "使用 $agent-fs-retrieval 通过内置 CLI 检索 Agent FS 知识库。用户只给云端地址时，先直接执行 connect-cloud，不要先看 CLI 帮助或脚本源码；如果云端缺 token 或认证失败，再补 connect-cloud --email/--password。只有在连接、本地运行时或环境本身失败时，才回退查看 setup.md。"
